# Portfolio image review — 4 September 2026

Reviewed the four public image assets in this website and the four current design images in `Mini34/pico-2w-power-transient-analyzer`.

- **Prototype overview:** replace inaccurate callouts, using the original photograph and controller/sensor close-ups. The image is an annotated explanatory view; the linked original photo remains the primary physical evidence.
- **Social preview:** retain. Its Signal & Self branding and abstract orbital artwork remain appropriate to the site's responsible-technology fieldbook. It makes no hardware or measurement claims.
- **World-map background:** retain as decorative artwork. The Evidence page supplies the sourced regional values separately in a table; the image itself is not a measured data map.
- **Favicon:** retain the existing native SVG mark.
- **QA screenshots:** retain as dated records of actual website layouts, separate from generated project illustrations.

In the Pico repository, retire the generated technical brief, generated timeline, ambiguous raster architecture, and their superseded design PDF bundle. Replace the architecture with a firmware-grounded Markdown diagram and retain all original source photographs. See the Pico repository's `docs/IMAGE_REVIEW.md` for item-by-item reasons.

Two built-in imagegen attempts were rejected for inaccurate endpoints and changes to photographed wiring. With user approval, the final image uses a deterministic editable overlay on the untouched source. The PNG and lossless WebP preserve every source pixel outside the annotation mask. The Pico repository includes the editable SVG and a reproducible builder with the five exact endpoint coordinates. Prompts for the rejected candidates are retained separately; no generated candidate is shipped.
